/*Foraneas*/
ALTER TABLE TELEFONOS ADD CONSTRAINT FK_TELEFONOS_CLIENTE 
  FOREIGN KEY (tid, nid) 
  REFERENCES CLIENTE(tid, nid);

ALTER TABLE BIEN ADD CONSTRAINT FK_BIEN_CLIENTE 
  FOREIGN KEY (tid, nid) 
  REFERENCES CLIENTE(tid, nid);
