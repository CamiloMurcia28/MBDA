/*Acciones*/
CONSTRAINT FK_TELEFONOS_CLIENTE FOREIGN KEY (tid, nid)
    REFERENCES CLIENTE(tid, nid)
    ON DELETE CASCADE;

CONSTRAINT FK_FACTURA_CLIENTE FOREIGN KEY (tid, nid)
    REFERENCES CLIENTE(tid, nid)
    ON DELETE CASCADE;
